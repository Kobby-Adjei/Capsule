# Capsule
Capsule is a shared context and record of what the user's agents are doing or have done.  The goal is to keep agents in sync so the user does not have to keep repeating or manually copying context between them.
